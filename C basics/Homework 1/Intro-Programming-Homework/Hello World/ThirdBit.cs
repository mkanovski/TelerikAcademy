using System;

namespace thirdBit
{
    class thirdBit
    {
        static void Main()
        {
            int value = int.Parse(Console.ReadLine());
            string binary = Convert.ToString(value, 2);

            Console.WriteLine(binary);
            Console.WriteLine(binary);
        }
    }
}
