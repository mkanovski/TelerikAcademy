using System;

namespace ThirdDigit
{
    class thirdDigit
    {
        static void Main()
        {
            int number = int.Parse(Console.ReadLine());
            if (number[3] == 7)
        }
    }
}
